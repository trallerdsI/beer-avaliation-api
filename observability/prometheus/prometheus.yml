global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "beer-avaliation-api"
    static_configs:
      - targets: ["app:8080"]
    metrics_path: /metrics
    scrape_interval: 5s
