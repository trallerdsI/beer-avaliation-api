# ======================================================================
# BEER REVIEW API - CONFIGURAÇÃO DE AMBIENTE (.env.example)
# ======================================================================
# Instruções: Copie este ficheiro, nomeie a cópia como ".env" e ajuste os valores.
# IMPORTANTE: Nunca envie o ficheiro ".env" real para o GitHub.

# ------------------------------------------------------------------------------
# CONFIGURAÇÕES DO SERVIDOR
# ------------------------------------------------------------------------------
SERVER_PORT=8082
# Nota: A Vercel atribui os portos dinamicamente em produção, mas mantenha esta
# variável para que o Viper não falhe nas validações locais.

# ------------------------------------------------------------------------------
# CONFIGURAÇÕES PARA AMBIENTE LOCAL (DOCKER / DEVELOPMENT)
# ------------------------------------------------------------------------------
# Estas três variáveis são consumidas pelo seu 'docker-compose.yaml' para provisionar o banco:
DB_USER=postgres
DB_PASSWORD=beer_secure_password123
DB_NAME=beer_review_db

# Esta é a string de ligação que a API em Go usará para se ligar ao Docker local:
DBConnString=postgres://postgres:beer_secure_password123@localhost:5432/beer_review_db?sslmode=disable

# ------------------------------------------------------------------------------
# CONFIGURAÇÕES PARA PRODUÇÃO (VERCEL + SUPABASE POSTGRES)
# ------------------------------------------------------------------------------
# ADVERTÊNCIA: Não coloque a string de produção neste ficheiro local!
# Adicione a DBConnString diretamente no painel da Vercel (Settings > Environment Variables).
#
# REGRA PARA SUPABASE + lib/pq (database/sql):
#   - Use a URL NON-POOLING (porta 5432), NÃO o pooler (porta 6543).
#     O PgBouncer em "transaction pooling" quebra prepared statements do driver.
#   - sslmode=require é obrigatório no Supabase.
# Exemplo do formato esperado na Vercel:
# DBConnString=postgres://postgres.SEU_PROJETO:PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require

# ------------------------------------------------------------------------------
# SEGURANÇA (JWT)
# ------------------------------------------------------------------------------
# Chave secreta para assinar e validar os tokens de login dos utilizadores.
# OBRIGATÓRIO: sem esta variável o serviço recusa gerar/validar tokens.
JWT_SECRET=substitua_isto_por_um_hash_aleatorio_e_seguro_em_producao

# ------------------------------------------------------------------------------
# POOL DE CONEXÕES (opcional, defaults: 10 open / 5 idle)
# ------------------------------------------------------------------------------
DB_MAX_OPEN_CONNS=10
DB_MAX_IDLE_CONNS=5

# ------------------------------------------------------------------------------
# CORS (produção)
# ------------------------------------------------------------------------------
# Origins permitidas separadas por vírgula. Use "*" apenas para dev/local.
# Exemplo: CORS_ALLOWED_ORIGINS=https://app.vercel.app,https://admin.vercel.app
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Regex opcional para permitir subdomínios dinamicamente.
# Exemplo: CORS_ALLOWED_REGEX=^https://(.*\.)?beer-avaliation-api\.vercel\.app$
# Se vazio, apenas a allowlist literal é usada.
CORS_ALLOWED_REGEX=

# ------------------------------------------------------------------------------
# STORAGE DE MÍDIA (upload de imagens — RFC 7578)
# ------------------------------------------------------------------------------
# Backend de object storage para as imagens enviadas pelo app Flutter.
# O Go ingere o binário, valida e grava aqui; devolve a URL pública.
# Sem estas variáveis o endpoint de upload responde 501 (resto da API intacto).
SUPABASE_URL=https://SEU_PROJETO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=substitua_pela_service_role_key_do_supabase
SUPABASE_STORAGE_BUCKET=beer-media

# ------------------------------------------------------------------------------
# RESET DESTRUTIVO DO ESQUEMA (banco como fonte de verdade)
# ------------------------------------------------------------------------------
# Quando true (default), a API recria o esquema do zero a cada arranque
# (internal/app/migrations/000_reset.sql faz DROP TABLE IF EXISTS ... CASCADE antes de
# recriar). Seguro enquanto o banco for descartável (sem dados definitivos).
# Defina como false (ou 0 / no) quando o banco tiver dados reais que NÃO devem
# ser apagados — nesse caso aplica-se apenas migrations incrementais
# (idempotentes com IF NOT EXISTS / IF EXISTS).
DB_RESET_SCHEMA=true

# ------------------------------------------------------------------------------
# OAuth2 / OIDC (login social) — RFC 6749
# Defina a AUDIENCE (client ID da vossa app Flutter) de cada IdP para ativar.
# Sem estas vars, POST /api/v1/users/oauth responde 400 (provider sem suporte).
# JWKS_URL é opcional (usa o endpoint público conhecido do issuer).
OAUTH_GOOGLE_AUDIENCE=
OAUTH_GOOGLE_JWKS_URL=
OAUTH_APPLE_AUDIENCE=
OAUTH_APPLE_JWKS_URL=

# ------------------------------------------------------------------------------
# WEB PUSH (notificações push) — RFC 8030
# ------------------------------------------------------------------------------
# Par de chaves VAPID (p256dh + privada) para autenticar o servidor de push.
# Gere com: npx web-push generate-vapid-keys
# Não precisa ser secreta absoluta, mas evite commit de keys de produção.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
